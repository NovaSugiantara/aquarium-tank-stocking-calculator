{
  "name": "aquarium-tank-stocking-calculator",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && cp index.html dist/index.html",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "devDependencies": { "@types/node": "^22.10.0", "typescript": "^5.6.3" }
}
